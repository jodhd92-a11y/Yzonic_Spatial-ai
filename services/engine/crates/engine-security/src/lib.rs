//! Security & policy layer — mTLS config, authz (RBAC→ABAC/OPA later),
//! audit-log derivation from the event log. Phase 4 (blueprint section 7)
//! per the "before any AI serving goes live, not after" ordering.
//! Empty placeholder in phase 1 — see crate doc note in engine-realtime
//! for why this exists now rather than being added later.
