//! Real-time hub — phase 2 (blueprint section 7, item 2): WebSocket/SSE
//! hub for chat streaming, wiring `apps/chat`'s `useSSEChat.ts` to a real
//! backend and retiring `mockResponder.ts`. Deliberately empty in phase 1
//! (week-1 scope is engine-core + engine-data + engine-auth only) — this
//! crate exists now so the workspace layout matches section 5 up front,
//! not because there's real logic here yet.
